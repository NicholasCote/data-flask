apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.webapp.name }}-preview
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.webapp.group }}
  annotations:
    cert-manager.io/cluster-issuer: "incommon"
spec:
  ingressClassName: traefik-internal
  tls:
    - hosts:
      - {{ .Values.webapp.preview.tls.fqdn }}
      secretName: {{ .Values.webapp.preview.tls.secretName }}
  rules:
  - host: {{ .Values.webapp.preview.tls.fqdn }}
    http:
      paths:
      - path: {{ .Values.webapp.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.webapp.name }}-preview
            port:
              number: {{ .Values.webapp.container.port }}