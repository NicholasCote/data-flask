apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-scripts
  namespace: {{ .Release.Namespace }}
data:
  init-db.sql: |
    CREATE DATABASE {{ .Values.db.name }};
    GRANT ALL PRIVILEGES ON DATABASE {{ .Values.db.name }} TO postgres;