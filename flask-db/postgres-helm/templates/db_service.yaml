apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.db.host }}
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.webapp.group }}
spec:
  type: ClusterIP
  ports:
  - port: 5432
  selector:
    app: {{ .Values.db.host }}